spring:
  application:
    name: tanzu-java-web-app
  datasource:
    url: jdbc:h2:mem:mydb
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"  
  endpoint:
    health:
      show-details: always
    